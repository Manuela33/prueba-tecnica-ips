# SQL Gold

| Archivo | Contenido |
|---------|-----------|
| `kpi_queries.sql` | Vistas `v_kpi_*`, `dim_paciente_bi` y ejemplos de consultas cortas |

El pipeline crea las mismas vistas vía `create_kpi_views()` en `03_gold_model.ipynb`.
